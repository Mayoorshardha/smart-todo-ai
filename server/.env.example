# Server Configuration
PORT=3001

# Optional: Set a default Claude API key (not recommended for security)
# CLAUDE_API_KEY=your_claude_api_key_here

# Note: It's more secure to pass the API key from the frontend
# The server accepts API keys via the x-api-key header